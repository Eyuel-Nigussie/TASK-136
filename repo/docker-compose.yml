# CourierMatch iOS -- Docker Compose
#
# Single command:
#   docker compose run build    Validate project structure and tests

services:
  build:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
